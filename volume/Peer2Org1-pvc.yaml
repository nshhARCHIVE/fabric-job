apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: ${K8S_NAMESPACE}
  name: peer2org1-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: peer2org1-pv
  resources:
    requests:
      storage: 1Gi